HITL/
│
├── graph.py        # Shared state, nodes, graph builder
├── before.py         # Run until human approval
├── human.py         # Human updates the state
└── after.py          # Resume execution after approval
